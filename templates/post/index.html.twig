{% extends 'base.html.twig' %}

{% block title %}ANIBLOG
{% endblock %}

{% block body %}
	<h1>Post index</h1>

	{% if show_overlay %}
		<section class="overlay">
			<section id="overlay-div">
				{% for post in posts %}
					<table class="table">
						<tbody>
							<tr>
								<th>Id</th>
								<td>{{ post.id }}</td>
							</tr>
							<tr>
								<th>Name</th>
								<td>{{ post.name }}</td>
							</tr>
							<tr>
								<th>Content</th>
								<td>{{ post.content }}</td>
							</tr>
							<tr>
								<th>CreatedAt</th>
								<td>{{ post.createdAt ? post.createdAt|date('Y-m-d H:i:s') : '' }}</td>
							</tr>
							<tr>
								<th>UpdatedAt</th>
								<td>{{ post.updatedAt ? post.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
							</tr>
							<tr>
								<th>Title</th>
								<td>{{ post.title }}</td>
							</tr>
						</tbody>
					</table>

					<a href="{{ path('app_post_index') }}">back to list</a>
					<a href="{{ path('app_post_edit', {'id': post.id}) }}">edit</a>
					{{ include('post/_delete_form.html.twig') }}
				{% endfor %}
			</section>
		</section>
	{% endif %}
	<section class="head-container">
		<a href="{{ path('app_post_new') }}">Create new</a>
	</section>
	<section class="main-container">
		<table class="table">
			<thead>
				<tr>
					<th>Id</th>
					<th>Name</th>
					<th>Content</th>
					<th>CreatedAt</th>
					<th>UpdatedAt</th>
					<th>Title</th>
				</tr>
			</thead>
		</br>
	</br>
</br></br><th>actions</th></tr></thead></br></br></br></br><tbody>{% for post in posts %}<tr><td>{{ post.id }}</td><td>{{ post.name }}</td><td>{{ post.content }}</td><td>{{ post.createdAt ? post.createdAt|date('Y-m-d H:i:s') : '' }}</td><td>{{ post.updatedAt ? post.updatedAt|date('Y-m-d H:i:s') : '' }}</td><td>{{ post.title }}</td><td><a href="{{ path('app_post_show', {'id': post.id}) }}">Show</a><a href="{{ path('app_post_edit', {'id': post.id}) }}">Edit</a><a href="{{ path('app_comment_new') }}">Nouveau commentaire</a></td></tr>{% else %}<tr><td colspan="7">no records found</td></tr>{% endfor %}</tbody></table></section> {% endblock %}
